# Copy to .env and edit DATABASE_URL accordingly
# Examples:
# SQLite (default local file):
# DATABASE_URL=sqlite:///data/resilientflow.db

# PostgreSQL (example):
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/resilientflow_db

# When using Docker, set DATABASE_URL via docker-compose environment or secret
